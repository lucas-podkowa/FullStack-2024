//forma clasica de declarar una funcion
function sumar(x, y) {
    console.log(x + y);
}

//podemos asignar una funcion a una variable
var adicion = function (x, y) {
    console.log(x + y);
}

sumar(4, 5); //llamamos a la funcion
adicion(4, 5); //llamamos a la funcion







